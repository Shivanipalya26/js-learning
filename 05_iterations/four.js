const myObj = {
    js : "Javascript", 
    cpp : "C++", 
    rb : "Ruby",
    sw : "Swift"
}

for (const key in myObj) {
    // console.log(`${key} is ${myObj[key]}`);
}

const arr = [3,4,5,6,7,8]

for (const key in arr) {
    // console.log(`${key} is ${arr[key]}`);
}

//map k upar iterations nhi kiya ja sakta
